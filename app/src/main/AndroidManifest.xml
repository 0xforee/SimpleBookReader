<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.foree.bookreader">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.foree.bookreader.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <service android:name="org.foree.bookreader.service.RefreshService" />

        <activity android:name="org.foree.bookreader.ui.activity.BookShelfActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.foree.bookreader.ui.activity.ChapterListActivity">
        </activity>
        <activity android:name="org.foree.bookreader.ui.activity.ArticleActivity"/>

        <!-- 配置第三方Receiver -->
        <receiver
            android:name="org.foree.bookreader.receiver.PushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.igexin.sdk.action.${GETUI_APP_ID}" />
            </intent-filter>
        </receiver>


    </application>

</manifest>